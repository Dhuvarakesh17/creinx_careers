import type { Config } from "tailwindcss";

export default {
  theme: {
    extend: {
      fontFamily: {
        display: ["Cabinet Grotesk", "sans-serif"],
        sans: ["Satoshi", "sans-serif"],
      },
    },
  },
} satisfies Config;
